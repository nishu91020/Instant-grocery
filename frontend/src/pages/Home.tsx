import React from 'react';
import { Typography } from '@mui/material';

const Home = () => {
    return (
        <div>
            <Typography>Home is here</Typography>
        </div>
    );
};

export default Home;
